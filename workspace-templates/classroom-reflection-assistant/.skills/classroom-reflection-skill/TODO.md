# TODO
+ 视频转文本流程✅：工具已写好，可以独立调用，并且被 skill 调用
  + 问题：有可能会超时。10min视频完成整体流程差不多花了 16min。
    + 通义听悟两种：实时/离线音频。
    + 目前使用实时推送，默认接近视频原时长，因为代码按实时速度推流。所以整体时间就很久。
    + 但离线生成需要 OSS 或ftp 服务器，需要一个可访问的公网。
+ 迁移成 ds-v4✅
+ 宋宇老师对接✅
  + 不要给分数，给定性的描述，不要给定量
  + 语音拿不到的细节，就不要出现在评价结果中，如对板书的评价
+ 评分表 公开课

# 已完成
+ 评分表（大致完成）
  + 筛选，每个学科1个通用模板
  + 转换成 markdown
  + 自动通过学科识别来匹配对应 评分表
+  优化当前输出结构
   +  环节：秒数描述到分钟描述（已完成：统一为“分钟 + 秒数”）
   +  教案结构：教学目标描述不要用三维目标，需要核心素养。基于文件构建核心素养json（已完成：新增 core-literacy-map.json）
+ 增加基于教案生成逐字稿（已完成：新增教师逐字稿 prompt、模板与落盘约定）
+ 测试自定义评分表
    + 已修复：按照skill的流程落地文件
+ 产出物增加生成时间标注，目录改为 `<conversation-id>` / 案例 id，避免同一次对话的报告、教案和逐字稿被拆到不同时间戳目录。（已完成）
  + 已修复：同一次对话中的输出报告和下一步的教案优化，在不同的时间输出目录下，有问题。
+ 核心素养、评分表根据资料优化
  + 核心素养细化✅，若为义务教育数学阶段则根据二级维度深入分析教学目标
+ 优化输出模板✅



# 疑问
+ 输出文件夹。脚本生成了很多中间内容。有些需要给用户隐藏 ✅
  + 在开发 skill 时可以暂时不考虑。是智能体平台的后续工作
+ 自测环节？